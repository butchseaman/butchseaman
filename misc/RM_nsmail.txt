#!/usr/bin/perl -w

#use strict;

$name=`hostname`;
$dates=`date +"%A, %B %d, %Y"`;

open(NSM, ">/home/cpe_os_user/NSM") || die;

print NSM "\n\n";
print NSM "Weekly update of available storage for Server: $name";
print NSM "\n\n";
print NSM "Available       NSMRM Storage Directory                   Percent Full\n";
print NSM "__________      _________________________________         ____________ \n\n";

#print NSM `df -kh | awk '{ print \$5 "           "   \$4 }' | grep FileNet`;
print NSM `df -kh | awk '{ print  \$3 "            "\$5 "      "   \$4 }' | grep NSMRM `;

$cclist='imaging.support@nationstarmail.com';

system (" cat NSM |/bin/mailx -s 'NSMRM Storage $name on $dates' 'butch.seaman\@nationstarmail.com' $cclist");

system ("/bin/cp /dev/null NSM");
close(NSM);